from setuptools import setup

setup(name = 'p-distributions-2024',
      version = '1',
      description = 'Package with probability distributions',
      packages=['p-distributions-2024'],
      zip_safe=False)
